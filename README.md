# Sales Data ETL Pipeline

This project demonstrates a simple ETL pipeline:

- Extracted structured sales data from CSV
- Transformed data (cleaning, type conversion, feature engineering)
- Loaded clean data into SQLite relational database
- Executed SQL queries for business insights

Technologies:
Python, Pandas, SQLite, SQL
